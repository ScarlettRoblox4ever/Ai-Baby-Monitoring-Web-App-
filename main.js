var r;
var g;
var b;



function setup() { 
  createCanvas(400, 400);
  rectMode(CENTER);
} 

function draw() { 
  
  background(220);
  
  r = random(255);
  g = random(100,200); 
  b = random(100); 
}